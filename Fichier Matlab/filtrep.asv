function [ output_args ] = filtrep( x0,y,f )
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here
T=length(y);
n=length(x0);
xf(:,1)=x0;
wftilda(:,1)=1/n*ones(n,1);
xf(:,t)=random('normal',mx(a,xf(:,t-1)),b);
for t=2:T
    


end

